#!/usr/bin/env yarn run repl -s
-- Deploys new Comptroller with some BEP20 and some vTokens

-- First deploy a price oracle
Gate (PriceOracle Address) (PriceOracle Deploy Feed)

-- Set Feed for known tokens
FeedPriceOracle SetFeed (WBTC Address) (WBTCFeed Address) 16

Print "Deployed FeedPriceOracle with known feeds" 
